

/** @type {import('next').NextConfig} */
const nextConfig = {

    images:{
        remotePatterns: [
            {
                hostname:"hearty-crocodile-183.convex.cloud",
            }
        ],
    },
};

export default nextConfig;
